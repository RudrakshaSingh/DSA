import java.util.Set;
import java.util.TreeSet;

public class j10Treeset {
  public static void main(String[] args) {
    Set<Integer> set = new TreeSet<>();
    set.size();
    // elemts stored are unique and in sorted (like stoored in binary search tree)
    // can use all 6 prop used in hash set
    // O(logN). like binary search tree operations

  }
}
